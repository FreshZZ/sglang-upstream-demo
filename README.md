# SGLang Upstream Demo

This repository simulates the SGLang upstream repository for 3-way merge demo.

## Structure

```
sglang/srt/managers/scheduler.py  # Scheduler class
```

## Tags

- `v1.0` - Initial version with basic FIFO scheduling
- `v1.1` - Added metrics in Step 5
- `v1.2` - Added prefill/decode separation in Step 2
